name: MLflow Server Docker Build & Push

on:
  push:
    branches: [ master ]
    paths:
      - 'Dockerfile.mlflow'
      - '.github/workflows/mlflow-build.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push MLflow server image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.mlflow
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mlflow-server:latest
            ghcr.io/${{ github.repository_owner }}/mlflow-server:${{ github.sha }}
          platforms: linux/amd64

      - name: Summary
        run: |
          echo "Pushed: ghcr.io/${{ github.repository_owner }}/mlflow-server:latest"